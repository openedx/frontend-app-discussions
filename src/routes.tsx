import Main from './Main';

// TODO: add all the current routes that exists in discussions home
const routes = [
  {
    id: 'org.openedx.frontend.route.discussions.main',
    path: '/',
    Component: Main,
  },
];

export default routes;
